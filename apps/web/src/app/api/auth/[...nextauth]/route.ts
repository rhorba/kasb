// S0-07 (Security Engineer): Auth.js v5 + Infobip OTP adapter
// This stub exports handlers to satisfy next-auth routing.
// Security Engineer replaces src/auth.ts with full implementation.
import { handlers } from "@/auth";

export const { GET, POST } = handlers;
